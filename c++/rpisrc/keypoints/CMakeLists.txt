cmake_minimum_required(VERSION 2.6)

FIND_PACKAGE(VXL REQUIRED)

INCLUDE(${VXL_CMAKE_DIR}/UseVXL.cmake)
INCLUDE_DIRECTORIES(${INCLUDE_DIRECTORIES} ${VXL_RPL_INCLUDE_DIR})

FIND_PACKAGE(RPI REQUIRED)
INCLUDE(${RPI_CMAKE_DIR}/UseRPI.cmake)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

PROJECT(keypoint)

ADD_EXECUTABLE(keypointExample keypointExample.cxx keypoint.cxx)
TARGET_LINK_LIBRARIES(keypointExample
vnl 
vgl 
rsdl 
rkpl
ITKIO
)

