cmake_minimum_required(VERSION 2.6)

PROJECT(AxisActor2D)

FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})

ADD_EXECUTABLE(AxisActor2D AxisActor2D.cxx)
TARGET_LINK_LIBRARIES(AxisActor2D vtkHybrid)

