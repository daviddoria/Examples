cmake_minimum_required(VERSION 2.6)

PROJECT(ImageRange3D)

FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})

ADD_EXECUTABLE(ImageRange3D ImageRange3D.cxx)
TARGET_LINK_LIBRARIES(ImageRange3D vtkHybrid)
