cmake_minimum_required(VERSION 2.6)

PROJECT(AllOperators)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(AllOperators AllOperators.cxx)
TARGET_LINK_LIBRARIES(AllOperators ${ITK_LIBRARIES})
