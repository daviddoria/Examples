cmake_minimum_required(VERSION 2.6)

PROJECT(BoundingBox)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(BoundingBox BoundingBox.cpp)
TARGET_LINK_LIBRARIES(BoundingBox ${ITK_LIBRARIES})