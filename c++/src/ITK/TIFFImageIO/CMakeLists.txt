cmake_minimum_required(VERSION 2.6)

PROJECT(TIFFImageIO)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(TIFFImageIO TIFFImageIO.cxx)
TARGET_LINK_LIBRARIES(TIFFImageIO
ITKIO ITKBasicFilters ITKCommon )
