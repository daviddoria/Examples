cmake_minimum_required(VERSION 2.6)

PROJECT(RegionGrowingMesh)

INCLUDE_DIRECTORIES(${INCLUDE_DIRECTORIES} 
/media/portable/ITK_Additions/itkFloodFillMesh
)

FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(RegionGrowingMesh RegionGrowingMesh.cpp)
TARGET_LINK_LIBRARIES(RegionGrowingMesh ITKIO)

