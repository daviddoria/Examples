cmake_minimum_required(VERSION 2.6)

PROJECT(Exceptions)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(Exceptions Exceptions.cxx)
TARGET_LINK_LIBRARIES(Exceptions ${ITK_LIBRARIES})
