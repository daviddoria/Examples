cmake_minimum_required(VERSION 2.6)

PROJECT(OutputMacros)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(OutputMacros OutputMacros.cxx)
TARGET_LINK_LIBRARIES(OutputMacros ${ITK_LIBRARIES})
