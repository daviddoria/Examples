cmake_minimum_required(VERSION 2.6)

PROJECT(XorImageFilter)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(XorImageFilter XorImageFilter.cxx)
TARGET_LINK_LIBRARIES(XorImageFilter ${ITK_LIBRARIES})

